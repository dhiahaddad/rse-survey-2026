---
title: "How should a national research software institution foster community engagement and knowledge transfer?"
question_id: org6de
---

```{r}
#| include: false
#| file: ../R/_load_common.R
```

```{r}
#| echo: false
#| fig-width: 10
#| fig-height: 6

plot_heatmap("Germany", "org6de", title = "Germany")
```

```{r}
#| echo: false
recode_map <- tibble::tribble(
  ~raw, ~clean,
)

cleaned_data <- process_text_codes_with_allocation(
  filter = "Germany",
  question_code = "org6de",
  recode_map = recode_map, 
  header = "RSE Institute - Community (Germany)",
  other = TRUE,
  cache_id = "org6de"
)

cleaned_data$summary_table
```

The raw-to-category allocation table for this question is in @sec-appendix-recoding.