---
title: "What would you hope to get out of such an organisation?"
question_id: org2can
---

```{r}
#| include: false
#| file: ../R/_load_common.R
```

```{r}
#| echo: false
#| fig-width: 10
#| fig-height: 6

plot_heatmap(FILTER, "org2can")
```

```{r}
#| echo: false
recode_map <- tibble::tribble(
  ~raw, ~clean,
)

cleaned_data <- process_text_codes_with_allocation(
  filter = FILTER,
  question_code = "org2can",
  recode_map = recode_map, 
  header = "Benefits",
  other = TRUE,
  cache_id = "org2can"
)

cleaned_data$summary_table
```

The raw-to-category allocation table for this question is in @sec-appendix-recoding.